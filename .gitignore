# === Dependencies ===
node_modules/

# === Build output ===
dist-react/

# === Development tools (local only) ===
.claude/
.moai/
.moai-backups/
CLAUDE.md
.claudeignore
.git_hooks/
# Agent scaffolding, not this project's CI. Committing it would ship the
# label-sync workflow and branch-protection template as live GitHub config,
# and land them in the deployed web directory (the server takes its copy via
# `git reset --hard origin/main`).
.github/

# === Environment & Secrets ===
.env
.env.*
*.env

# === OS & Editor ===
.DS_Store
Thumbs.db
*.swp
*.swo
*~
.idea/
.vscode/

# === Logs ===
*.log
nohup.out

# === Project management (local only) ===
PROJECT_HANDOFF.md
PROJECT_LOG.md

# No-paste resume prompt (machine-local, per-session)
/NEXT_SESSION.md
